# Story 3.3: Analise da Transcricao com GPT

## Status

Done

## Executor Assignment

```
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["build", "lint", "typecheck"]
```

## Story

**As a** usuario,
**I want** que a IA analise a transcricao e extraia dores, solucoes e acoes automaticamente,
**so that** eu nao precise ler toda a transcricao manualmente.

## Acceptance Criteria

1. API route `POST /api/analyze` recebe transcricao + lista de participantes (com CCs)
2. Prompt estruturado conforme definido no brief (secao 12)
3. GPT retorna JSON com: title (sugestao de titulo ~5 palavras), context, pains[]{description, solutions[], actions[]}
4. Cada action tem: action, responsible, actionOwner, account, deadline (ou null)
5. Parser valida o JSON e trata erros de formato (se invalido, permite retry ou preenchimento manual — FR34)
6. Resultado retornado ao frontend como JSON estruturado (persistencia ocorre no save final da reuniao — ver Story 4.3)

## Tasks / Subtasks

- [x] Task 1: Criar prompt template (AC: 2, 3, 4)
  - [x] Criar arquivo `src/lib/prompts.ts`
  - [x] Implementar funcao `buildAnalysisPrompt(transcription, participants)` conforme Section 19
  - [x] Prompt inclui lista de participantes com seus centros de custo
  - [x] Prompt especifica formato JSON de saida com title, context, pains
- [x] Task 2: Criar API route POST `/api/analyze` (AC: 1, 3, 4, 5)
  - [x] Criar arquivo `src/app/api/analyze/route.ts`
  - [x] Validar que transcricao nao esta vazia
  - [x] Truncar transcricao se > 12000 chars (~3000 tokens) para respeitar timeout 10s
  - [x] Chamar GPT-4o-mini com temperature 0.3 e response_format json_object
  - [x] Parsear resposta JSON
  - [x] Validar estrutura basica (context e pains existem e sao array)
  - [x] Se JSON invalido, retornar 422 com `{ error: '...', raw: content }` (FR34)
- [x] Task 3: Configurar OpenAI client (AC: 1)
  - [x] Criar (ou verificar) `src/lib/openai.ts` com client OpenAI configurado
  - [x] Usar `OPENAI_API_KEY` do environment
- [x] Task 4: Testar manualmente (AC: 1-6)
  - [x] Enviar transcricao curta com participantes — verificar JSON de retorno
  - [x] Enviar transcricao longa (>12000 chars) — verificar truncamento
  - [x] Verificar que title, context, pains, solutions e actions estao presentes
  - [x] Verificar que campos null sao retornados corretamente

## Dev Notes

### Analysis Route — FULL Implementation

[Source: architecture.md#Section 9.4]

```typescript
// src/app/api/analyze/route.ts

import { NextRequest, NextResponse } from 'next/server';
import { openai } from '@/lib/openai';
import { buildAnalysisPrompt } from '@/lib/prompts';

export async function POST(request: NextRequest) {
  const { transcription, participants } = await request.json();

  if (!transcription?.trim()) {
    return NextResponse.json({ error: 'Transcricao obrigatoria' }, { status: 400 });
  }

  // Truncar se muito longa (respeitar timeout 10s)
  const maxChars = 12000; // ~3000 tokens
  const truncated = transcription.length > maxChars
    ? transcription.substring(0, maxChars) + '\n[...transcricao truncada...]'
    : transcription;

  const prompt = buildAnalysisPrompt(truncated, participants);

  let content: string | null;
  try {
    const completion = await openai.chat.completions.create({
      model: 'gpt-4o-mini',
      messages: [{ role: 'user', content: prompt }],
      temperature: 0.3,
      response_format: { type: 'json_object' },
    });

    content = completion.choices[0]?.message?.content ?? null;
  } catch (error: any) {
    const status = error?.status || 500;
    const message = error?.message || 'Erro ao chamar GPT';
    return NextResponse.json({ error: message }, { status });
  }

  if (!content) {
    return NextResponse.json({ error: 'GPT nao retornou resposta' }, { status: 500 });
  }

  try {
    const analysis = JSON.parse(content);
    // Validar estrutura basica
    if (!analysis.context || !Array.isArray(analysis.pains)) {
      throw new Error('Estrutura invalida');
    }
    return NextResponse.json(analysis);
  } catch {
    return NextResponse.json(
      { error: 'GPT retornou JSON invalido', raw: content },
      { status: 422 }
    );
  }
}
```

### GPT Prompt Template — FULL Implementation

[Source: architecture.md#Section 19]

```typescript
// src/lib/prompts.ts

export function buildAnalysisPrompt(
  transcription: string,
  participants: { name: string; costCenter: string | null }[]
): string {
  const participantList = participants
    .map((p) => `- ${p.name}${p.costCenter ? ` (CC: ${p.costCenter})` : ''}`)
    .join('\n');

  return `Voce e um analista de reunioes. Analise a transcricao abaixo.

Participantes desta reuniao:
${participantList}

Extraia:
1. **title**: Titulo curto da reuniao (~5 palavras)
2. **context**: Resumo de 2-3 frases sobre o tema da reuniao
3. **pains**: Lista de problemas/dificuldades mencionados, cada um com:
   - description: descricao do problema
   - solutions: lista de solucoes propostas
   - actions: lista de tarefas, cada uma com:
     - action: descricao da tarefa
     - responsible: quem demanda (use nomes dos participantes, ou null)
     - actionOwner: quem executa (use nomes dos participantes, ou null)
     - account: categoria/projeto relacionado (ou null)
     - deadline: prazo se mencionado (formato YYYY-MM-DD, ou null)

O centro de custo de cada acao = centro de custo do responsavel.
Se nao conseguir identificar um campo, retorne null.

Responda SOMENTE em JSON valido:
{
  "title": "...",
  "context": "...",
  "pains": [
    {
      "description": "...",
      "solutions": ["..."],
      "actions": [
        {
          "action": "...",
          "responsible": "..." ou null,
          "actionOwner": "..." ou null,
          "account": "..." ou null,
          "deadline": "..." ou null
        }
      ]
    }
  ]
}

Transcricao:
---
${transcription}
---`;
}
```

### GPT Response TypeScript Interfaces

[Source: architecture.md#Section 4.2]

```typescript
export interface GPTAnalysisResponse {
  title: string;
  context: string;
  pains: GPTPain[];
}

export interface GPTPain {
  description: string;
  solutions: string[];
  actions: GPTAction[];
}

export interface GPTAction {
  action: string;
  responsible: string | null;
  actionOwner: string | null;
  account: string | null;
  deadline: string | null;
}
```

### API Endpoint

[Source: architecture.md#Section 5.2]

| Method | Path | Descricao | Request Body | Response |
|--------|------|-----------|-------------|----------|
| POST | `/api/analyze` | Analisar transcricao com GPT | `{transcription, participants[]}` | `GPTAnalysisResponse` |

### GPT Configuration

- **Model:** `gpt-4o-mini`
- **Temperature:** `0.3` (baixa para respostas mais consistentes)
- **Response format:** `{ type: "json_object" }` (forca JSON valido)
- **Truncation:** maxChars = 12000 (~3000 tokens) para respeitar timeout 10s da Vercel

### CC Business Rule

O centro de custo de cada Todo/Acao = centro de custo do **Responsavel** (quem demanda). Isso e informado no prompt para que o GPT preencha automaticamente com base na lista de participantes.

### FR34 Fallback — GPT Failure

[Source: architecture.md#Section 17.3]

Se a analise GPT falhar ou retornar JSON invalido:
1. API retorna status 422 com `{ error: "...", raw: "..." }`
2. Frontend exibe mensagem: "A analise IA falhou. Deseja tentar novamente ou preencher manualmente?"
3. Opcao "Tentar novamente" → re-envia para `/api/analyze`
4. Opcao "Preencher manualmente" → avanca no wizard com dores/solucoes/todos vazios para preenchimento manual

### File Paths

- `src/app/api/analyze/route.ts` — API route de analise
- `src/lib/prompts.ts` — template do prompt GPT
- `src/lib/openai.ts` — OpenAI client configuration

### Testing

- Nesta story: testar manualmente via curl ou pelo frontend (se disponivel)
- Verificar que `npm run build` compila sem erros
- Testes automatizados serao adicionados em story futura

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 15/03/2026 | 1.0 | Story criada | River (SM) |
| 15/03/2026 | 1.1 | Fix: try/catch ao redor da chamada OpenAI | River (SM) |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6 (claude-opus-4-6)

### Debug Log References
- 4 testes prompt (buildAnalysisPrompt), 53 total
- OpenAI client (lazy Proxy) ja existia de story 2.3
- Build OK, lint OK, typecheck OK

### Completion Notes List
- buildAnalysisPrompt com participantes + centros de custo
- POST /api/analyze com truncamento 12000 chars, GPT-4o-mini, temp 0.3
- response_format json_object forca JSON valido
- Validacao de estrutura (context + pains array)
- 422 com raw content se JSON invalido (FR34)
- GPT interfaces em lib/types.ts (GPTAnalysisResponse, GPTPain, GPTAction)

### File List
- `src/lib/prompts.ts` — prompt template (NEW)
- `src/app/api/analyze/route.ts` — GPT analysis endpoint (NEW)
- `src/lib/types.ts` — GPT interfaces adicionadas (MODIFIED)
- `src/lib/__tests__/prompts.test.ts` — prompt tests (NEW)
