# Story 4.5: Home com Historico de Reunioes

## Status

Done

## Executor Assignment

```
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["build", "lint", "typecheck"]
```

## Story

**As a** usuario,
**I want** ver todas as minhas reunioes anteriores na pagina inicial,
**so that** eu encontre facilmente uma reuniao especifica.

## Acceptance Criteria

1. Pagina Home (`/`) lista reunioes como cards (MeetingCard)
2. Card mostra: titulo (auto-gerado ou editado), data, quantidade de to-dos, status resumido
3. Campo de busca filtra por texto na transcricao (via `GET /api/meetings?search=`)
4. Ordenacao por data (mais recente primeiro)
5. Botao "Nova Gravacao" proeminente no topo
6. Link para Dashboard no header/navegacao

## Tasks / Subtasks

- [x] Task 1: Criar componente MeetingCard (AC: 2)
  - [x] Criar `src/components/MeetingCard.tsx`
  - [x] Exibir titulo (fallback "Reuniao sem titulo" se null)
  - [x] Exibir data formatada (ex: "15 Mar 2026")
  - [x] Exibir quantidade de to-dos via `_count.todos`
  - [x] Card clicavel — link para `/meeting/[id]`
  - [x] Usar shadcn/ui Card como base
- [x] Task 2: Atualizar pagina Home (AC: 1, 4, 5)
  - [x] Atualizar `src/app/page.tsx` (substituir placeholder da Story 1.3)
  - [x] Usar `'use client'` (busca interativa)
  - [x] Fetch reunioes via `GET /api/meetings` no useEffect
  - [x] Renderizar lista de MeetingCard
  - [x] Ordenacao por data descendente (ja vem da API)
  - [x] Botao "Nova Gravacao" proeminente no topo, link para `/recording`
  - [x] Empty state quando nao existem reunioes
- [x] Task 3: Implementar busca (AC: 3)
  - [x] Campo de busca (Input) no topo da lista
  - [x] Debounce de 300ms no input
  - [x] Ao digitar, re-fetch via `GET /api/meetings?search={query}`
  - [x] Limpar busca restaura lista completa
- [x] Task 4: Garantir navegacao no header (AC: 6)
  - [x] Verificar/adicionar link para Dashboard no Header (componente Header.tsx)
  - [x] Verificar link para Home e Nova Gravacao no header
- [x] Task 5: Testar pagina (AC: 1-6)
  - [x] Verificar listagem de reunioes
  - [x] Verificar busca com debounce
  - [x] Verificar empty state
  - [x] Verificar navegacao para detalhes ao clicar no card
  - [x] Verificar que build e lint passam

## Dev Notes

### Arquivos a Criar/Atualizar

```
src/components/MeetingCard.tsx    # Componente card de reuniao (CRIAR)
src/app/page.tsx                  # Pagina Home (ATUALIZAR — substituir placeholder)
```

### MeetingListItem Type

[Source: architecture.md#Section 4.2]

```typescript
// lib/types.ts

export interface MeetingListItem {
  id: string;
  title: string | null;
  date: Date;
  createdAt: Date;
  _count: { todos: number };
}
```

### Component Hierarchy

[Source: architecture.md#Section 8.2]

```
HomePage (/)
├── SearchInput (campo de busca com debounce)
├── Button "Nova Gravacao" → link /recording
└── MeetingCard[] → link para /meeting/[id]
```

### API Utilizada

```typescript
// GET /api/meetings?search= retorna MeetingListItem[]
// Busca usa contains + mode: 'insensitive' (ILIKE) na transcricao

const meetings = await prisma.meeting.findMany({
  where: search
    ? { transcription: { contains: search, mode: 'insensitive' as const } }
    : {},
  select: {
    id: true,
    title: true,
    date: true,
    createdAt: true,
    _count: { select: { todos: true } },
  },
  orderBy: { date: 'desc' },
});
```

### Debounce na Busca

[Source: architecture.md#Section 14.2]

Campos de busca devem usar debounce de 300ms para evitar requests excessivos:

```typescript
// Implementacao simples de debounce
const [search, setSearch] = useState('');
const [debouncedSearch, setDebouncedSearch] = useState('');

useEffect(() => {
  const timer = setTimeout(() => {
    setDebouncedSearch(search);
  }, 300);
  return () => clearTimeout(timer);
}, [search]);

useEffect(() => {
  // Fetch meetings com debouncedSearch
  fetchMeetings(debouncedSearch);
}, [debouncedSearch]);
```

### Empty State

Quando nao existem reunioes, exibir mensagem amigavel:

```tsx
{meetings.length === 0 && !loading && (
  <div className="text-center py-12">
    <p className="text-muted-foreground mb-4">
      Nenhuma reuniao encontrada.
    </p>
    <Button onClick={() => router.push('/recording')}>
      Gravar primeira reuniao
    </Button>
  </div>
)}
```

**Nota:** shadcn/ui v4 NAO suporta `asChild` no Button — usar `onClick` + `router.push`.

### Navegacao (Header)

[Source: architecture.md#Section 8.2]

```
RootLayout (layout.tsx)
├── Header (nav: Home, Dashboard, Nova Gravacao)
```

O header deve conter links para:
- `/` — Home (lista de reunioes)
- `/dashboard` — Dashboard consolidado
- `/recording` — Nova Gravacao

### Frontend Services Layer

[Source: architecture.md#Section 8.5]

```typescript
// lib/api.ts
export async function getMeetings(search?: string): Promise<MeetingListItem[]> {
  const params = search ? `?search=${encodeURIComponent(search)}` : '';
  const res = await fetch(`/api/meetings${params}`);
  if (!res.ok) throw new Error('Failed to fetch meetings');
  return res.json();
}
```

### Restricoes

- **`'use client'`** obrigatorio na pagina Home (busca interativa)
- **shadcn/ui v4**: Button NAO suporta `asChild` — usar `onClick` + `router.push` para navegacao
- **Debounce 300ms** obrigatorio no campo de busca
- **Ordenacao por data descendente** — mais recente primeiro (ja vem ordenado da API)

### Testing

- Verificar listagem de reunioes com cards
- Verificar busca por texto com debounce
- Verificar empty state quando nao ha reunioes
- Verificar navegacao card → meeting detail
- Verificar botao "Nova Gravacao" navega para /recording
- Verificar que `npm run build` e `npm run lint` passam

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 15/03/2026 | 1.0 | Story criada | River (SM) |
| 15/03/2026 | 1.1 | Fix: removido anti-padrao asChild no empty state | Pax (PO) |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6

### Debug Log References
- No issues encountered

### Completion Notes List
- MeetingCard component with title fallback, formatted date, todo count, clickable link
- Home page with client-side fetch from GET /api/meetings
- Search with 300ms debounce, re-fetches on debouncedSearch change
- Empty state with different messages for no meetings vs no search results
- Loading spinner while fetching
- Header already had Home, Dashboard, Nova Gravação links (Story 1.3)
- MeetingListItem type added to types.ts
- Build OK, lint OK, typecheck OK, 93 tests passing

### File List
| File | Action | Description |
|------|--------|-------------|
| src/lib/types.ts | Modified | Added MeetingListItem interface |
| src/components/MeetingCard.tsx | Created | Meeting card component with title, date, todo count |
| src/app/page.tsx | Modified | Full home page with search, debounce, meeting list |
