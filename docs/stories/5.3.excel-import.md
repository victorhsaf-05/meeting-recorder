# Story 5.3: Importacao de Excel

## Status

Done

## Executor Assignment

```
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["build", "lint", "typecheck"]
```

## Story

**As a** usuario,
**I want** importar planilhas Excel existentes para o app,
**so that** eu migre meus dados historicos sem redigitar tudo.

## Acceptance Criteria

1. Componente `ExcelImport` com upload de arquivo .xlsx
2. Interface de mapeamento de colunas: usuario mapeia colunas do Excel para colunas do app
3. Preview dos dados antes de confirmar importacao
4. API route `POST /api/import` processa o arquivo
5. Importacao respeita campo Status da planilha (nao sobrescreve com "Pendente")
6. Cria Meeting associada aos to-dos importados (com titulo e data)
7. Nomes de responsaveis da planilha sao adicionados ao cadastro de participantes se nao existirem
8. Tratamento de erros (colunas faltantes, dados invalidos)

## Tasks / Subtasks

- [x] Task 1: Criar componente ExcelImport (AC: 1, 2, 3)
  - [x] Criar `src/components/ExcelImport.tsx`
  - [x] Implementar upload de arquivo via `onClick` + hidden `<input type="file">` (shadcn/ui v4 pattern)
  - [x] Usar SheetJS para ler o arquivo .xlsx no client e extrair headers + rows
  - [x] Implementar interface de mapeamento de colunas: headers do Excel na esquerda, colunas do app na direita com Select dropdowns
  - [x] Colunas do app: Responsavel, Resp. pela acao, Centro de custo, Data reuniao, Conta, TO-DO, Prazo, Status
  - [x] Implementar preview dos dados mapeados em tabela antes de confirmar
  - [x] Campos obrigatorios antes de enviar: titulo da reuniao, data, e pelo menos a coluna TO-DO mapeada
- [x] Task 2: Criar API route de importacao (AC: 4, 5, 6, 7)
  - [x] Criar `src/app/api/import/route.ts` com handler POST
  - [x] Validar body como `ImportExcelRequest`
  - [x] Executar em `prisma.$transaction`:
    - [x] Extrair nomes unicos de `responsible` e `actionOwner`
    - [x] Upsert cada nome em Participant (criar se nao existe)
    - [x] Criar Meeting com titulo e data
    - [x] Criar MeetingParticipant links
    - [x] Criar Todos vinculados ao Meeting (respeitando status da planilha)
  - [x] Retornar Meeting completo com relacoes
- [x] Task 3: Tratamento de erros (AC: 8)
  - [x] Validar que pelo menos a coluna "TO-DO" (action) esta mapeada
  - [x] Tratar dados invalidos (datas malformadas, campos vazios)
  - [x] Exibir mensagens de erro claras ao usuario
  - [x] Feedback visual de sucesso apos importacao
- [x] Task 4: Integrar no dashboard (AC: 1)
  - [x] Substituir placeholder da Story 5.1 pelo componente `ExcelImport` funcional
  - [x] Apos importacao com sucesso, re-fetch dos dados do dashboard
- [x] Task 5: Verificar build e lint
  - [x] Executar `npm run build` — deve compilar sem erros
  - [x] Executar `npm run lint` — deve passar sem erros

## Dev Notes

### Arquivos a Criar/Modificar

- `src/components/ExcelImport.tsx` — Componente de upload + mapeamento + preview
- `src/app/api/import/route.ts` — API route de importacao
- `src/lib/excel.ts` — Adicionar funcoes de leitura de Excel (se necessario)
- `src/app/dashboard/page.tsx` — Integrar ExcelImport (substituir placeholder da Story 5.1)

### TypeScript Interface: ImportExcelRequest

[Source: architecture.md#Section 4.2]

```typescript
export interface ImportExcelRequest {
  title: string;
  date: string;
  todos: {
    action: string;
    responsible?: string;
    actionOwner?: string;
    costCenter?: string;
    account?: string;
    deadline?: string;
    meetingDate?: string;
    status?: TodoStatus;
  }[];
}
```

### Fluxo de Importacao Completo

[Source: architecture.md#Section 20.2]

1. **Client:** SheetJS le o `.xlsx` e extrai headers + rows
2. **Client:** Interface de mapeamento — usuario mapeia colunas do Excel para colunas do app
3. **Client:** Preview dos dados mapeados
4. **Client:** Envia dados mapeados para `POST /api/import` (`ImportExcelRequest`)
5. **Server:** Executa em transacao:
   a. Extrai nomes unicos de `responsible` e `actionOwner` de todos os todos
   b. Para cada nome, faz `upsert` no Participant (cria se nao existe, ignora se ja existe)
   c. Cria Meeting com titulo e data
   d. Cria MeetingParticipant links para todos os participantes encontrados
   e. Cria Todos vinculados ao Meeting (respeitando status da planilha — FR27)
   f. Retorna Meeting completo com relacoes

### CRITICAL: Auto-criacao de Participantes (FR6)

[Source: architecture.md#Section 20.2]

A rota de import garante que todos os nomes de `responsible` e `actionOwner` sao adicionados ao cadastro de participantes automaticamente via `upsert`. Isso e consistente com o comportamento do `ParticipantSelector` no wizard. Exemplo de upsert:

```typescript
// Dentro da transacao
const uniqueNames = [...new Set([
  ...todos.map(t => t.responsible).filter(Boolean),
  ...todos.map(t => t.actionOwner).filter(Boolean),
])] as string[];

const participants = [];
for (const name of uniqueNames) {
  const participant = await tx.participant.upsert({
    where: { name },
    update: {},
    create: { name },
  });
  participants.push(participant);
}
```

### CRITICAL: Transacao Atomica com prisma.$transaction

Toda a operacao de importacao DEVE ser executada dentro de `prisma.$transaction` para garantir atomicidade. Se qualquer etapa falhar (ex: upsert de participante, criacao de Meeting, criacao de Todos), tudo e revertido.

### CRITICAL: Respeitar Status da Planilha (FR27)

A importacao NAO deve sobrescrever o campo `status` com "Pendente". Se a planilha tiver uma coluna de status mapeada, o valor original deve ser preservado. Se nao houver coluna de status mapeada, ai sim usar "Pendente" como default (que ja e o default do schema Prisma).

### Interface de Mapeamento de Colunas

Layout da interface:

```
+-----------------------------+----------------------------+
| Coluna do Excel             | Coluna do App              |
+-----------------------------+----------------------------+
| "Nome Responsavel"          | [Select: Responsavel    v] |
| "Pessoa Acao"               | [Select: Resp. pela acao v] |
| "CC"                        | [Select: Centro de custo v] |
| "Data"                      | [Select: Data reuniao    v] |
| "Account"                   | [Select: Conta           v] |
| "Acao"                      | [Select: TO-DO           v] |
| "Due Date"                  | [Select: Prazo           v] |
| "Situacao"                  | [Select: Status          v] |
| "Observacoes"               | [Select: (nao mapear)    v] |
+-----------------------------+----------------------------+
```

Colunas do app disponiveis para mapeamento:
- Responsavel
- Resp. pela acao
- Centro de custo
- Data reuniao
- Conta
- TO-DO
- Prazo
- Status
- (nao mapear)

### Client-Side Parsing com SheetJS

```typescript
import * as XLSX from 'xlsx';

// Ler arquivo no client
function readExcelFile(file: File): Promise<{ headers: string[]; rows: Record<string, string>[] }> {
  return new Promise((resolve, reject) => {
    const reader = new FileReader();
    reader.onload = (e) => {
      const data = new Uint8Array(e.target!.result as ArrayBuffer);
      const workbook = XLSX.read(data, { type: 'array' });
      const sheetName = workbook.SheetNames[0];
      const worksheet = workbook.Sheets[sheetName];
      const jsonData = XLSX.utils.sheet_to_json<Record<string, string>>(worksheet, { header: 1 });

      const headers = jsonData[0] as unknown as string[];
      const rows = jsonData.slice(1).map((row: any) => {
        const obj: Record<string, string> = {};
        headers.forEach((h, i) => { obj[h] = row[i] || ''; });
        return obj;
      });

      resolve({ headers, rows });
    };
    reader.onerror = reject;
    reader.readAsArrayBuffer(file);
  });
}
```

### shadcn/ui v4 Button File Upload Pattern

[Source: architecture.md#Section 3.1]

shadcn/ui v4 NAO suporta prop `asChild` no Button. Para file uploads, usar o pattern `onClick` + hidden `<input type="file">`:

```typescript
const fileInputRef = useRef<HTMLInputElement>(null);

<Button onClick={() => fileInputRef.current?.click()}>
  Importar Excel
</Button>
<input
  ref={fileInputRef}
  type="file"
  accept=".xlsx,.xls"
  className="hidden"
  onChange={handleFileChange}
/>
```

### Restricoes Tecnicas

[Source: architecture.md#Section 3.1, 3.2]

- **shadcn/ui v4**: NAO suporta prop `asChild` no Button. Usar `onClick` + hidden `<input type="file">`
- **Select.onValueChange**: Pode retornar null. Guard com `(v) => v && handler(v)`
- **TypeScript strict mode**: Obrigatorio
- **prisma.$transaction**: Obrigatorio para atomicidade da importacao
- **Client Component**: `ExcelImport` deve usar `'use client'` pois SheetJS manipula o DOM
- **Vercel body limit**: O request body de `POST /api/import` nao pode exceder 4.5MB. Como o parsing do Excel e feito no client (SheetJS) e apenas o JSON mapeado e enviado para a API, o tamanho do body depende do numero de linhas. Para planilhas muito grandes (>10000 linhas), considerar enviar em lotes ou exibir aviso ao usuario

### Testing

- Verificar que upload de arquivo .xlsx funciona
- Verificar que headers do Excel sao extraidos corretamente
- Verificar que interface de mapeamento exibe todas as colunas
- Verificar que preview mostra dados mapeados corretamente
- Verificar que importacao cria Meeting + Todos no banco
- Verificar que participantes sao criados via upsert (nomes novos)
- Verificar que status da planilha e preservado (nao sobrescrito com "Pendente")
- Verificar tratamento de erros: arquivo invalido, colunas faltantes
- `npm run build` deve compilar sem erros
- `npm run lint` deve passar sem erros

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 15/03/2026 | 1.0 | Story criada | River (SM) |
| 15/03/2026 | 1.1 | Fix: typecheck adicionado, nota sobre Vercel body limit | River (SM) |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6

### Debug Log References
- Fixed TS2367 error: `v === '__none__'` comparison needed cast `as string` for type compatibility

### Completion Notes List
- ExcelImport component with 3-step flow: upload → mapping → preview
- File upload via onClick + hidden input pattern (shadcn/ui v4)
- Client-side Excel parsing with SheetJS (readExcelFile in lib/excel.ts)
- Column mapping UI with Select dropdowns for each Excel header
- Preview shows up to 10 mapped rows before confirmation
- POST /api/import route with prisma.$transaction for atomicity
- Participant upsert: creates new participants from responsible/actionOwner names
- Respects status from spreadsheet (FR27), defaults to Pendente if unmapped
- ImportExcelRequest type added to types.ts
- Integrated in dashboard, re-fetches data after successful import
- Build OK, lint OK, typecheck OK, 93 tests passing

### File List
| File | Action | Description |
|------|--------|-------------|
| src/lib/types.ts | Modified | Added ImportExcelRequest interface |
| src/lib/excel.ts | Modified | Added readExcelFile function |
| src/components/ExcelImport.tsx | Created | 3-step import component (upload/mapping/preview) |
| src/app/api/import/route.ts | Created | POST handler with transaction, participant upsert |
| src/app/dashboard/page.tsx | Modified | Replaced placeholder with ExcelImport component |
