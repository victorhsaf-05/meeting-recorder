# Story 3.1: CRUD de Participantes

## Status

Done

## Executor Assignment

```
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["build", "lint", "typecheck"]
```

## Story

**As a** usuario,
**I want** cadastrar participantes com nome e centro de custo,
**so that** a IA saiba quem participou da reuniao e seus centros de custo.

## Acceptance Criteria

1. API route `GET /api/participants` para listar participantes (ordenados por nome)
2. API route `POST /api/participants` para criar participante
3. API route `PUT /api/participants/[id]` para atualizar nome, centro de custo ou cargo
4. API route `DELETE /api/participants/[id]` para remover participante
5. Participante tem: nome (unico), centro de custo (opcional), cargo (opcional)
6. Nome duplicado retorna erro com mensagem clara

## Tasks / Subtasks

- [x] Task 1: Criar API route GET/POST `/api/participants` (AC: 1, 2, 5, 6)
  - [x] Criar arquivo `src/app/api/participants/route.ts`
  - [x] Implementar handler `GET` — `prisma.participant.findMany({ orderBy: { name: 'asc' } })`
  - [x] Implementar handler `POST` — validar nome obrigatorio, criar com `prisma.participant.create`
  - [x] Tratar erro Prisma P2002 (unique constraint) retornando 409 com code `DUPLICATE_NAME`
  - [x] Usar helper `apiError` de `@/lib/utils`
- [x] Task 2: Criar API route PUT/DELETE `/api/participants/[id]` (AC: 3, 4)
  - [x] Criar arquivo `src/app/api/participants/[id]/route.ts`
  - [x] Implementar handler `PUT` — atualizar nome, costCenter e/ou role
  - [x] Tratar erro P2002 no PUT (caso atualize nome para um ja existente)
  - [x] Implementar handler `DELETE` — `prisma.participant.delete({ where: { id } })`
  - [x] Tratar erro P2025 (record not found) retornando 404
- [x] Task 3: Adicionar helper `apiError` em `src/lib/utils.ts` (AC: 6)
  - [x] Implementar funcao `apiError(message, status, code?)` conforme Section 17.1
- [x] Task 4: Testar manualmente todas as rotas (AC: 1-6)
  - [x] GET retorna lista vazia `[]` inicialmente
  - [x] POST cria participante e retorna 201
  - [x] POST com nome duplicado retorna 409 com `{ error: '...', code: 'DUPLICATE_NAME' }`
  - [x] PUT atualiza campos e retorna participante atualizado
  - [x] DELETE remove participante e retorna `{ success: true }`
  - [x] GET apos operacoes retorna lista ordenada por nome

## Dev Notes

### API Endpoints — Participants

[Source: architecture.md#Section 5.2]

| Method | Path | Descricao | Request Body | Response |
|--------|------|-----------|-------------|----------|
| GET | `/api/participants` | Listar todos (ordenados por nome) | — | `Participant[]` |
| POST | `/api/participants` | Criar participante | `{name, costCenter?, role?}` | `Participant` |
| PUT | `/api/participants/[id]` | Atualizar participante | `{name?, costCenter?, role?}` | `Participant` |
| DELETE | `/api/participants/[id]` | Remover participante | — | `{success: true}` |

### API Route Pattern — participants/route.ts

[Source: architecture.md#Section 9.1]

```typescript
// src/app/api/participants/route.ts

import { NextRequest, NextResponse } from 'next/server';
import { prisma } from '@/lib/db';

export async function GET() {
  const participants = await prisma.participant.findMany({
    orderBy: { name: 'asc' },
  });
  return NextResponse.json(participants);
}

export async function POST(request: NextRequest) {
  const body = await request.json();

  // Validacao
  if (!body.name?.trim()) {
    return NextResponse.json(
      { error: 'Nome e obrigatorio' },
      { status: 400 }
    );
  }

  try {
    const participant = await prisma.participant.create({
      data: {
        name: body.name.trim(),
        costCenter: body.costCenter?.trim() || null,
        role: body.role?.trim() || null,
      },
    });
    return NextResponse.json(participant, { status: 201 });
  } catch (error: any) {
    if (error.code === 'P2002') {
      return NextResponse.json(
        { error: 'Participante com este nome ja existe', code: 'DUPLICATE_NAME' },
        { status: 409 }
      );
    }
    throw error;
  }
}
```

### Error Response Format

[Source: architecture.md#Section 5.3]

```typescript
interface ApiErrorResponse {
  error: string;    // Mensagem legivel
  code?: string;    // Codigo de erro (ex: "DUPLICATE_NAME", "INVALID_FORMAT")
  details?: any;    // Detalhes extras (ex: campo invalido)
}

// HTTP Status Codes usados:
// 200 - Success
// 201 - Created
// 400 - Bad Request (validacao)
// 404 - Not Found
// 409 - Conflict (ex: nome duplicado)
// 500 - Internal Server Error
```

### apiError Helper

[Source: architecture.md#Section 17.1]

```typescript
// lib/utils.ts

export function apiError(message: string, status: number, code?: string) {
  return NextResponse.json(
    { error: message, ...(code && { code }) },
    { status }
  );
}

// Uso nas routes:
// return apiError('Nome obrigatorio', 400);
// return apiError('Participante ja existe', 409, 'DUPLICATE_NAME');
```

### Prisma Participant Model

[Source: architecture.md#Section 7.1]

```prisma
model Participant {
  id         String               @id @default(cuid())
  name       String               @unique
  costCenter String?
  role       String?
  createdAt  DateTime             @default(now())
  updatedAt  DateTime             @updatedAt
  meetings   MeetingParticipant[]
}
```

### File Paths

- `src/app/api/participants/route.ts` — GET (list) + POST (create)
- `src/app/api/participants/[id]/route.ts` — PUT (update) + DELETE (remove)
- `src/lib/utils.ts` — adicionar helper `apiError`

### Error Handling

- Prisma error code `P2002` = unique constraint violation → return 409 with `{ error: 'Participante com este nome ja existe', code: 'DUPLICATE_NAME' }`
- Prisma error code `P2025` = record not found → return 404 with `{ error: 'Participante nao encontrado' }`
- Nome vazio ou ausente → return 400 with `{ error: 'Nome e obrigatorio' }`

### Cascade Delete — Comportamento Importante

[Source: architecture.md#Section 7.1]

Ao deletar um participante via `DELETE /api/participants/[id]`, os registros em `MeetingParticipant` vinculados a este participante sao **automaticamente removidos** (onDelete: Cascade). Isso significa que o participante sera desvinculado de todas as reunioes, mas as reunioes em si NAO sao deletadas. Este e o comportamento desejado.

### Testing

- Nesta story: testar manualmente via curl ou Postman
- Testes automatizados serao adicionados em story futura

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 15/03/2026 | 1.0 | Story criada | River (SM) |
| 15/03/2026 | 1.1 | Fix: nota sobre cascade-delete de MeetingParticipant | River (SM) |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6 (claude-opus-4-6)

### Debug Log References
- Build OK, lint OK, typecheck OK
- 25 testes passando (regressao completa)
- Next.js 16 async params pattern usado em [id] routes
- Prisma error typing com `error instanceof Object && 'code' in error`

### Completion Notes List
- GET /api/participants — lista ordenada por nome
- POST /api/participants — validacao nome + P2002 → 409 DUPLICATE_NAME
- PUT /api/participants/[id] — update parcial, P2002/P2025 tratados
- DELETE /api/participants/[id] — cascade em MeetingParticipant, P2025 → 404
- apiError helper em lib/utils.ts

### File List
- `src/lib/utils.ts` — adicionado helper apiError (MODIFIED)
- `src/app/api/participants/route.ts` — GET + POST (NEW)
- `src/app/api/participants/[id]/route.ts` — PUT + DELETE (NEW)
