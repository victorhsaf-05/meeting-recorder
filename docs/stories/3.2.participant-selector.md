# Story 3.2: Seletor de Participantes na Tela de Gravacao

## Status

Done

## Executor Assignment

```
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["build", "lint", "typecheck"]
```

## Story

**As a** usuario,
**I want** selecionar os participantes da reuniao antes de gravar,
**so that** a IA tenha contexto sobre quem esta falando.

## Acceptance Criteria

1. Componente `ParticipantSelector` com dropdown searchable
2. Lista participantes cadastrados com nome e centro de custo
3. Opcao de digitar nome novo — ao digitar nome que nao existe, oferecer "Adicionar [nome]"
4. Ao adicionar novo participante, perguntar centro de custo
5. Participantes selecionados aparecem como chips/badges removiveis
6. Manter selecao de participantes no client state (persistencia via save final da reuniao — Story 4.3)

## Tasks / Subtasks

- [x] Task 1: Criar componente `ParticipantSelector` (AC: 1, 2, 5)
  - [x] Criar arquivo `src/components/ParticipantSelector.tsx`
  - [x] Implementar dropdown searchable usando shadcn/ui Popover + Command (ou Input + Popover)
  - [x] Listar participantes existentes via `getParticipants()` do service layer
  - [x] Exibir nome e centro de custo na lista
  - [x] Renderizar participantes selecionados como Badge (chips) removiveis
- [x] Task 2: Implementar adicao de novo participante inline (AC: 3, 4)
  - [x] Ao digitar nome que nao existe na lista, exibir opcao "Adicionar [nome]"
  - [x] Ao clicar em "Adicionar", exibir campo para centro de custo (opcional)
  - [x] Criar participante IMEDIATAMENTE via `POST /api/participants` (registro global)
  - [x] Ao receber resposta com o participante criado, adicioná-lo a selecao
  - [x] Tratar erro 409 (DUPLICATE_NAME) — participante ja existe, selecionar o existente
- [x] Task 3: Implementar funcoes do service layer (AC: 1, 3)
  - [x] Criar (ou atualizar) `src/lib/api.ts` com funcoes `getParticipants()` e `createParticipant()`
- [x] Task 4: Integrar com Wizard state (AC: 6)
  - [x] Componente recebe `selectedParticipants` e `onSelectionChange` como props
  - [x] O wizard armazena apenas os `participantIds` (e objetos Participant para exibicao) no client state
  - [x] O link `MeetingParticipant` e criado no save final (POST /api/meetings — Story 4.3)
- [x] Task 5: Testar cenarios (AC: 1-6)
  - [x] Buscar participante existente no dropdown
  - [x] Selecionar participante — aparece como badge
  - [x] Remover participante selecionado — badge desaparece
  - [x] Digitar nome novo — opcao "Adicionar [nome]" aparece
  - [x] Adicionar novo participante com centro de custo — criado via API + selecionado

## Dev Notes

### CRITICAL: Participante e Registro Global (NAO faz parte do wizard client state)

[Source: architecture.md#Section 8.3]

O cadastro de participantes e **global** — nao faz parte dos dados da reuniao. Quando o usuario digita um nome novo no `ParticipantSelector`, o componente cria o participante **imediatamente** via `POST /api/participants` e recebe o ID de volta. Isso nao viola o FR30 (client state ate save) porque:

- FR30 refere-se aos dados da reuniao (transcricao, pains, todos)
- Participantes sao cadastro global reutilizavel entre reunioes
- O wizard armazena apenas os `participantIds` selecionados no client state
- O link `MeetingParticipant` e criado no save final (`POST /api/meetings`)

### Component Structure

```
src/components/ParticipantSelector.tsx
```

**Props esperadas:**

```typescript
interface ParticipantSelectorProps {
  selectedParticipants: Participant[];
  onSelectionChange: (participants: Participant[]) => void;
}
```

### Wizard Integration

[Source: architecture.md#Section 8.3]

No `RecordingPage`, o `ParticipantSelector` e o Step 1 do wizard:

```
RecordingPage (/recording) — WIZARD
├── WizardStepper (indicador de etapas)
├── Step 1: ParticipantSelector    ← ESTA STORY
├── Step 2: AudioRecorder | AudioUpload
├── Step 3: TranscriptionView
├── ...
```

O WizardState armazena:
```typescript
// De WizardState (Section 4.2)
selectedParticipants: Participant[];
```

A acao do reducer:
```typescript
| { type: 'SET_PARTICIPANTS'; participants: Participant[] }
```

### Frontend API Service Layer

[Source: architecture.md#Section 8.5]

```typescript
// lib/api.ts

const API_BASE = '/api';

// Participants
export async function getParticipants(): Promise<Participant[]> { ... }
export async function createParticipant(data: {name: string; costCenter?: string; role?: string}): Promise<Participant> { ... }
export async function updateParticipant(id: string, data: Partial<Participant>): Promise<Participant> { ... }
export async function deleteParticipant(id: string): Promise<void> { ... }
```

### shadcn/ui Components Usados

- **Popover** + **Command** (ou **Input** + lista filtrada): dropdown searchable
- **Badge**: chips removiveis dos participantes selecionados
- **Input**: campo de busca/digitacao
- **Button**: botao de remover no badge, botao "Adicionar"

### Participant TypeScript Interface

[Source: architecture.md#Section 4.2]

```typescript
export interface Participant {
  id: string;
  name: string;
  costCenter: string | null;
  role: string | null;
  createdAt: Date;
  updatedAt: Date;
}
```

### File Paths

- `src/components/ParticipantSelector.tsx` — componente principal
- `src/lib/api.ts` — funcoes `getParticipants()` e `createParticipant()`

### Testing

- Nesta story: testar manualmente no browser
- Verificar que `npm run build` compila sem erros
- Testes automatizados serao adicionados em story futura

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 15/03/2026 | 1.0 | Story criada | River (SM) |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6 (claude-opus-4-6)

### Debug Log References
- Usou Input + dropdown list customizado (mais simples que Popover+Command combo)
- Badge do shadcn v4 usa useRender de @base-ui
- 6 testes (ParticipantSelector), 49 total
- Build OK, lint OK, typecheck OK

### Completion Notes List
- ParticipantSelector com Input searchable + dropdown list
- Participantes selecionados como Badge removiveis
- Opcao "Adicionar [nome]" para nomes novos com campo centro de custo
- Cria participante via POST /api/participants + seleciona automaticamente
- Trata erro 409 DUPLICATE_NAME selecionando o existente
- Service layer em lib/api.ts (getParticipants, createParticipant, updateParticipant, deleteParticipant)
- Interface Participant em lib/types.ts

### File List
- `src/components/ParticipantSelector.tsx` — componente seletor (NEW)
- `src/lib/api.ts` — service layer frontend (NEW)
- `src/lib/types.ts` — Participant interface (MODIFIED)
- `src/components/__tests__/ParticipantSelector.test.tsx` — testes (NEW)
