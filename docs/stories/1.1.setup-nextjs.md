# Story 1.1: Setup do Projeto Next.js com TypeScript e Tailwind

## Status

Done

## Executor Assignment

```
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["build", "lint", "typecheck"]
```

## Story

**As a** desenvolvedor,
**I want** um projeto Next.js configurado com TypeScript, Tailwind CSS e shadcn/ui,
**so that** tenhamos a base do frontend pronta para desenvolvimento.

## Acceptance Criteria

1. Projeto Next.js 14+ criado com App Router e TypeScript strict mode
2. Tailwind CSS configurado e funcionando
3. shadcn/ui inicializado com componentes base (Button, Input, Card, Select, Table, Badge)
4. Estrutura de pastas conforme arquitetura definida (app/, components/, lib/)
5. `npm run dev` inicia sem erros
6. `npm run build` compila sem erros

## Tasks / Subtasks

- [x] Task 1: Criar projeto Next.js (AC: 1)
  - [x] Executar `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir` na pasta meeting-recorder
  - [x] Verificar que `tsconfig.json` esta com strict mode habilitado
  - [x] Verificar que App Router esta sendo usado (pasta `src/app/`)
- [x] Task 2: Configurar shadcn/ui (AC: 2, 3)
  - [x] Executar `npx shadcn@latest init`
  - [x] Adicionar componentes: `npx shadcn@latest add button input card select table badge dialog dropdown-menu calendar popover progress command`
- [x] Task 3: Criar estrutura de pastas (AC: 4)
  - [x] Criar `src/components/` (ja existira com ui/)
  - [x] Criar `src/hooks/`
  - [x] Criar `src/lib/`
  - [x] Criar `src/lib/types.ts` (arquivo vazio ou com comentario placeholder)
  - [x] Criar `src/lib/utils.ts` (ja existira do shadcn)
- [x] Task 4: Criar arquivo .env.example (AC: 4)
  - [x] Conteudo: `DATABASE_URL="postgresql://..."` e `OPENAI_API_KEY="sk-..."`
- [x] Task 5: Verificar build (AC: 5, 6)
  - [x] Executar `npm run dev` — deve iniciar sem erros
  - [x] Executar `npm run build` — deve compilar sem erros

## Dev Notes

### Estrutura de Pastas Alvo

[Source: architecture.md#Section 11]

```
src/
├── app/                          # Next.js App Router
│   ├── layout.tsx                # Root layout
│   ├── page.tsx                  # Home (placeholder por enquanto)
│   ├── globals.css               # Tailwind globals
│   └── api/                      # API Routes (vazio por enquanto)
├── components/
│   └── ui/                       # shadcn/ui (auto-gerado)
├── hooks/                        # Custom hooks (vazio por enquanto)
└── lib/
    ├── types.ts                  # TypeScript interfaces (placeholder)
    └── utils.ts                  # Utilidades (criado pelo shadcn)
```

### Restricoes Tecnicas

[Source: architecture.md#Section 3.1]

- **shadcn/ui v4**: NAO suporta prop `asChild` no Button. Para file uploads usar `onClick` + hidden `<input type="file">`
- **Select.onValueChange**: Pode retornar null. Guard com `(v) => v && handler(v)`
- **TypeScript strict mode**: Obrigatorio

### Comandos de Setup

[Source: architecture.md#Section 12.2]

```bash
npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir
npx shadcn@latest init
npx shadcn@latest add button input card select table badge dialog dropdown-menu calendar popover progress command
```

### Testing

- Framework: Vitest + React Testing Library (sera configurado em story futura)
- Nesta story: apenas verificar que `npm run dev` e `npm run build` funcionam

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 15/03/2026 | 1.0 | Story criada | River (SM) |
| 15/03/2026 | 1.1 | Fix: progress e command adicionados ao shadcn install, typecheck adicionado | River (SM) |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6 (claude-opus-4-6)

### Debug Log References
- Next.js 16.1.6 criado com Turbopack
- shadcn/ui v4 com Tailwind CSS v4
- 14 componentes UI instalados (button, input, card, select, table, badge, dialog, dropdown-menu, calendar, popover, progress, command, textarea, input-group)
- Build OK, lint OK, typecheck OK

### Completion Notes List
- Projeto criado com `create-next-app@latest` — Next.js 16.1.6
- TypeScript strict mode confirmado em tsconfig.json
- shadcn/ui v4 inicializado com todos os componentes requeridos
- Estrutura de pastas criada: src/app/, src/components/ui/, src/hooks/, src/lib/
- .env.example criado com DATABASE_URL e OPENAI_API_KEY

### File List
- `package.json` — dependencias do projeto
- `tsconfig.json` — TypeScript config (strict: true)
- `next.config.ts` — Next.js config
- `postcss.config.mjs` — PostCSS config
- `eslint.config.mjs` — ESLint config
- `components.json` — shadcn/ui config
- `.env.example` — template de variaveis de ambiente (NEW)
- `src/app/layout.tsx` — root layout
- `src/app/page.tsx` — home page (placeholder)
- `src/app/globals.css` — Tailwind globals
- `src/lib/utils.ts` — utilidades (cn helper do shadcn)
- `src/lib/types.ts` — TypeScript interfaces placeholder (NEW)
- `src/components/ui/button.tsx` — shadcn Button
- `src/components/ui/input.tsx` — shadcn Input
- `src/components/ui/card.tsx` — shadcn Card
- `src/components/ui/select.tsx` — shadcn Select
- `src/components/ui/table.tsx` — shadcn Table
- `src/components/ui/badge.tsx` — shadcn Badge
- `src/components/ui/dialog.tsx` — shadcn Dialog
- `src/components/ui/dropdown-menu.tsx` — shadcn DropdownMenu
- `src/components/ui/calendar.tsx` — shadcn Calendar
- `src/components/ui/popover.tsx` — shadcn Popover
- `src/components/ui/progress.tsx` — shadcn Progress
- `src/components/ui/command.tsx` — shadcn Command
- `src/components/ui/textarea.tsx` — shadcn Textarea (bonus)
- `src/components/ui/input-group.tsx` — shadcn InputGroup (bonus)
- `src/hooks/` — diretorio vazio (placeholder)
